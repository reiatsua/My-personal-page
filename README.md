![Python](https://img.shields.io/badge/python-3.14+-blue.svg) ![Django](https://img.shields.io/badge/django-6.0.2-green.svg)

# Моя Личная Страница

Личный веб-сайт с информацией о себе, достижениях и школе.

## Технологический стек

- **Backend**: Django 6.0.2
- **Frontend**: Bootstrap 5.3.3
- **База данных**: SQLite
- **Python**: 3.14+

## Требования

- Python 3.8 или выше
- pip (менеджер пакетов Python)

## Установка

### 1. Клонирование или загрузка проекта (можете скачать архив и через консоль перейти к проекту)

```bash
cd путь/к/проекту
```

### 2. Создание виртуального окружения (рекомендуется)

```bash
python -m venv venv
```

Активация виртуального окружения:

**Windows:**
```bash
venv\Scripts\activate
```

**macOS/Linux:**
```bash
source venv/bin/activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

Или вручную:
```bash
pip install Django==6.0.2
pip install Pillow
pip install whitenoise==6.12.0
pip install gunicorn
pip install requests>=2.32.0
```

## Запуск проекта

### 1. Применение миграций базы данных

```bash
python manage.py migrate
```

### 2. Создание суперпользователя (администратора)

```bash
python manage.py createsuperuser
```

Введите:
- Имя пользователя (username)
- Email
- Пароль (минимум 8 символов)

### 3. Запуск сервера разработки

```bash
python manage.py runserver
```

Сервер запустится по адресу: **http://127.0.0.1:8000/**

## Использование

### Главная страница
- http://127.0.0.1:8000/ - Страница "Обо мне"
- http://127.0.0.1:8000/achievements/ - Мои достижения
- http://127.0.0.1:8000/school/ - Моя школа
- http://127.0.0.1:8000/weather/ - Погода в Петропавловске

### Администраторская панель
- http://127.0.0.1:8000/admin/ - Вход в админ-панель
- Введите учетные данные суперпользователя

**В админ-панели вы можете:**
- Редактировать информацию "Обо мне" (имя, дату рождения, достижения и т.д.)
- Добавлять фотографии и видео достижений
- Добавлять фотографии и видео школы
- Управлять всеми данными сайта

## Структура проекта

```
my_page/
├── main/                    # Основное приложение Django
│   ├── models.py           # Модели базы данных
│   ├── views.py            # Представления (views)
│   ├── urls.py             # URL маршруты
│   ├── admin.py            # Конфигурация админ-панели
│   ├── templatetags/       # Пользовательские теги шаблонов
│   └── migrations/         # Миграции базы данных
├── personal_site/          # Настройки проекта Django
│   ├── settings.py         # Основные настройки
│   ├── urls.py             # Главные URL маршруты
│   └── wsgi.py             # WSGI конфигурация
├── templates/              # HTML шаблоны
├── static/                 # Статические файлы (CSS, JS, изображения)
├── media/                  # Загруженные медиафайлы (фото, видео)
├── db.sqlite3              # База данных SQLite
├── manage.py               # Команды управления Django
├── requirements.txt        # Список зависимостей для установки
├── Procfile                # Файл для деплоя
└── README.md               # Этот файл
```

## Функции

✨ **Страница "Обо мне":**
- Фотография профиля
- Биография (в виде нумерованного списка)
- Школа
- Ссылка на GitHub
- Увлечения
- Родные (в виде маркированного списка)
- Друзья с фотографией класса
- Гороскоп
- Пожелания (с разноцветными словами)

🏆 **Страница "Достижения":**
- Галерея достижений с карточками
- Подробная страница каждого достижения
- Карусель фотографий для каждого достижения

🏫 **Страница "Школа":**
- Описание школы
- Кнопка для перехода на сайт школы
- Карусель фотографий и видео школы

🌤️ **Страница "Погода":**
- Дата
- Состояние
- Температура (в градусах Цельсия)
- Скорость ветра (в метрах в секунду)

## Решение проблем

### Ошибка: "Module not found"
Убедитесь, что вы создали виртуальное окружение и установили зависимости:
```bash
pip install -r requirements.txt
```

### Ошибка: "Database locked"
Удалите `db.sqlite3` и запустите миграции заново:
```bash
rm db.sqlite3
python manage.py migrate
python manage.py createsuperuser
```

### Медиафайлы не загружаются
Убедитесь, что папка `media/` существует и имеет права на запись.

### Статические файлы не загружаются в продакшене
Запустите:
```bash
python manage.py collectstatic
```

## Дополнительно

- **Фоновое изображение**: Сохранено в `media/backgrounds/bgviolet.avif`
- **Формат фотографий**: Поддерживаются JPG, PNG, WebP, AVIF
- **Формат видео для школы**: Поддерживаются MP4, WebM, OGG

## Автор

Маляуцкий Кирилл 10 "Б" класс

## Лицензия

Проект свободен для использования в образовательных целях.
